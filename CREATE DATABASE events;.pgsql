
-- CREATE TABLE events (
--     event_id serial PRIMARY KEY,
--     event_name varchar(255) NOT NULL,
--     event_date date NOT NULL,
--     event_location varchar(255) NOT NULL,
--     event_promo varchar(255)
-- );

-- INSERT INTO events(event_name, event_date, event_location, event_promo)
-- VALUES(
--     'Habo',
--     '12/01/00',
--     'Outside',
--     '/images/habo.png'
-- )

SELECT * from events